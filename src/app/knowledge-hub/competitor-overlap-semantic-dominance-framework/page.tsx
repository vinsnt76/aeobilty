import CompetitorOverlapFrameworkArticlePage, { metadata as pageMetadata } from '../articles/competitor-overlap-semantic-dominance-framework/page';

export const metadata = {
  ...pageMetadata,
  alternates: {
    canonical: "https://aeobility.com.au/knowledge-hub/articles/competitor-overlap-semantic-dominance-framework",
  },
};

export default function AlternateCompetitorOverlapPage() {
  return <CompetitorOverlapFrameworkArticlePage />;
}
